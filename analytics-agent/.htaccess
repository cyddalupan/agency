RewriteEngine On
RewriteBase /analytics-agent/

# Redirect all requests to index.html unless it's a file or directory that exists
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /analytics-agent/index.html [L]