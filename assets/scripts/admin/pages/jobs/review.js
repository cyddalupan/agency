/* Script */

jQuery(function() {
	$(document).ready(function() {
        
        // $('input.fee-amount').on('keyup', function() {
        //     var amount = 0;
        //     var dollar = 0;

        //     dollar = $('input[name="dollar_exchange"]').val();
        //     alert(dollar);

        //     $('input.fee-amount').each(function(index, element) {

        //         if ( ! $.isNumeric( parseFloat( $(this).val() ) )  ) {
        //           continue;
        //         }

        //         var cost = parseFloat( $(this).val() );

        //         if ( cost == 0 ) {
        //           continue;
        //         }

        //         if ( $(this).hasClass('with-dollar') ) {
        //           cost = cost * dollar;
        //         }

        //         amount += cost;
        //     });
            
        //     $('.total-revenue').text( amount.toFixed(2) );    
        // });
        
        
		
	}); //endOf: $(document).ready
	
}); //endOf: jQuery